name: Auto Comment on new issues
on:
  issues:
    type: [opened]

jobs:
   comment-with-action:
     runs-on: ubuntu-latest
     steps:
      - name: "Github Context"
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
